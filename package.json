{
  "author": "Ryan Volz <rvolz@mit.edu>",
  "name": "widget",
  "license": "MIT OR Apache-2.0",
  "scripts": {
    "wasm": "cd bindings && wasm-pack build --target web",
    "predev": "npm run wasm",
    "prebuild": "npm run wasm",
    "dev": "vite build --watch",
    "build": "vite build"
  },
  "type": "module",
  "devDependencies": {
    "@anywidget/vite": "^0.2.2",
    "esbuild": "^0.25.4",
    "vite": "^7.3.1",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.5.0"
  },
  "dependencies": {
    "mqtt": "^5.15.0"
  }
}