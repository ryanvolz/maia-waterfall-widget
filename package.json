{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.js --minify --format=esm --bundle --outdir=src/maia_waterfall_widget/static"
	},
	"dependencies": {},
	"devDependencies": {
		"esbuild": "^0.25.4"
	}
}