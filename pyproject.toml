[project]
authors = [{name = "Ryan Volz", email = ""}]
dependencies = []
name = "maia-waterfall-widget"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["https://prefix.dev/conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
maia_waterfall_widget = { path = ".", editable = true }

[tool.pixi.tasks]
